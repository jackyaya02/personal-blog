# 数据库连接字符串（必需）
# 格式：mysql://用户名:密码@主机:端口/数据库名
DATABASE_URL="mysql://root:password@localhost:3306/blog_db"

# JWT 签名密钥（必需）
# 生产环境请使用足够长的随机字符串，例如：openssl rand -base64 32
JWT_SECRET="dev-secret-please-change-in-production"

# 站点 URL（可选，用于 sitemap 和 SEO）
NEXT_PUBLIC_APP_URL="http://localhost:3000"
